{
	"name": "woocommerce-paraguay-shipping",
	"title": "YITH WAPO Import Export Plugin",
	"description": "A plugin to import/export product addons from YITH WooCommerce Product Add-Ons & Extra Options",
	"version": "1.0.0",
	"scripts": {
		"prebuild": "rd /s /q ./vendor",
		"build": "composer install -o --no-dev && npm run archive",
		"build:dev": "composer install -o",
		"archive": "composer archive --file=$npm_package_name --format=zip",
		"postarchive": "rm -rf $npm_package_name && unzip $npm_package_name.zip -d $npm_package_name && rm $npm_package_name.zip && zip -r $npm_package_name.zip $npm_package_name && rm -rf $npm_package_name",
		"bump": "node version-bump.js",
		"bump:major": "node version-bump.js major"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/ferroche90/woocommerce-paraguay-shipping.git"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/ferroche90/woocommerce-paraguay-shipping/issues"
	},
	"homepage": "https://github.com/ferroche90/woocommerce-paraguay-shipping#readme"
}